global:
  novuVersion: 1.0.0
api: { ingress: { enabled: true, hosts: [{ host: novu-api.gridforge.internal }] } }
web: { ingress: { enabled: true, hosts: [{ host: novu.gridforge.internal }] } }
mongodb: { enabled: true }
redis:   { enabled: true }
