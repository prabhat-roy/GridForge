catalog:
  name: gridforge_lake
  type: rest
  uri: https://nessie.gridforge.internal/iceberg
warehouse: s3://gridforge-data-lake/warehouse
io:
  s3:
    endpoint: https://s3.amazonaws.com
    region: us-east-1
table-default:
  write.format.default: parquet
  write.metadata.compression-codec: gzip
