opencost:
  prometheus: { internal: { enabled: false }, external: { url: http://prometheus.observability:9090 } }
  ui: { enabled: true }
  exporter:
    cloudProviderApiKey: ""
    defaultClusterId: gridforge-prod
ingress:
  enabled: true
  hosts: [{ host: opencost.gridforge.internal, paths: [{ path: /, pathType: Prefix }] }]
