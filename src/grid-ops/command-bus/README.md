# command-bus

OT command bus with break-glass approval

Domain: ``grid-ops`` Â· Language: rust Â· Port: 50044

> Every IT->OT command MUST present a signed authorisation token. Break-glass mode requires
> a 4-eyes approval (NERC CIP-007 R5). All command dispatches are logged immutably.
