tag: 2.36.0
host: defectdojo.gridforge.internal
extraEnv:
  - { name: DD_DATABASE_URL, valueFrom: { secretKeyRef: { name: defectdojo-postgres, key: url } } }
redis: { enabled: true }
celery:
  worker: { replicaCount: 2 }
ingress:
  enabled: true
  hosts: [{ host: defectdojo.gridforge.internal, paths: [{ path: /, pathType: Prefix }] }]
