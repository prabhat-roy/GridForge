apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata: { name: gridforge-vault-issuer }
spec:
  vault:
    server: https://vault.gridforge.internal
    path: pki/sign/gridforge-mesh
    auth:
      kubernetes:
        role: cert-manager
        mountPath: /v1/auth/kubernetes
        secretRef: { name: cert-manager-vault-token, key: token }
