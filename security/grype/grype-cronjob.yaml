apiVersion: batch/v1
kind: CronJob
metadata: { name: grype-scan, namespace: gridforge-security }
spec:
  schedule: "0 2 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: grype
              image: anchore/grype:latest
              args: ["registry:harbor.gridforge.internal/gridforge","-o","json"]
