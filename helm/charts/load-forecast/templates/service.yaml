apiVersion: v1
kind: Service
metadata:
  name: {{ include "load-forecast.fullname" . }}
  labels: {{- include "load-forecast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "load-forecast.selectorLabels" . | nindent 4 }}
