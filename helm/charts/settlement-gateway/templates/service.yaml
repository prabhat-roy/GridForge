apiVersion: v1
kind: Service
metadata:
  name: {{ include "settlement-gateway.fullname" . }}
  labels: {{- include "settlement-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "settlement-gateway.selectorLabels" . | nindent 4 }}
