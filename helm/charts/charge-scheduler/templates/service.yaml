apiVersion: v1
kind: Service
metadata:
  name: {{ include "charge-scheduler.fullname" . }}
  labels: {{- include "charge-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "charge-scheduler.selectorLabels" . | nindent 4 }}
