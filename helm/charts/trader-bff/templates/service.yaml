apiVersion: v1
kind: Service
metadata:
  name: {{ include "trader-bff.fullname" . }}
  labels: {{- include "trader-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "trader-bff.selectorLabels" . | nindent 4 }}
