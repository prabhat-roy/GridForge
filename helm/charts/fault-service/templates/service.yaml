apiVersion: v1
kind: Service
metadata:
  name: {{ include "fault-service.fullname" . }}
  labels: {{- include "fault-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fault-service.selectorLabels" . | nindent 4 }}
