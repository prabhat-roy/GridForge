apiVersion: v1
kind: Service
metadata:
  name: {{ include "crew-dispatch.fullname" . }}
  labels: {{- include "crew-dispatch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "crew-dispatch.selectorLabels" . | nindent 4 }}
