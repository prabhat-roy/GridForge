apiVersion: v1
kind: Service
metadata:
  name: {{ include "offset-service.fullname" . }}
  labels: {{- include "offset-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "offset-service.selectorLabels" . | nindent 4 }}
