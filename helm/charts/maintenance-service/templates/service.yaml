apiVersion: v1
kind: Service
metadata:
  name: {{ include "maintenance-service.fullname" . }}
  labels: {{- include "maintenance-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "maintenance-service.selectorLabels" . | nindent 4 }}
