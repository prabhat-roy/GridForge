apiVersion: v1
kind: Service
metadata:
  name: {{ include "dr-program.fullname" . }}
  labels: {{- include "dr-program.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dr-program.selectorLabels" . | nindent 4 }}
