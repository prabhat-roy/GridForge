apiVersion: v1
kind: Service
metadata:
  name: {{ include "plant-service.fullname" . }}
  labels: {{- include "plant-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "plant-service.selectorLabels" . | nindent 4 }}
