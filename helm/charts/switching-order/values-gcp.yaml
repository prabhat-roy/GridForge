cloud:
  provider: gcp
  storageClass: pd-balanced
  ingressClass: gce

serviceAccount:
  annotations:
    iam.gke.io/gcp-service-account: gridforge-switching-order@{{ .Values.gcp.project }}.iam.gserviceaccount.com
