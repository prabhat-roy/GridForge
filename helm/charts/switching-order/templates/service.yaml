apiVersion: v1
kind: Service
metadata:
  name: {{ include "switching-order.fullname" . }}
  labels: {{- include "switching-order.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "switching-order.selectorLabels" . | nindent 4 }}
