apiVersion: v1
kind: Service
metadata:
  name: {{ include "command-bus.fullname" . }}
  labels: {{- include "command-bus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "command-bus.selectorLabels" . | nindent 4 }}
