apiVersion: v1
kind: Service
metadata:
  name: {{ include "der-service.fullname" . }}
  labels: {{- include "der-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "der-service.selectorLabels" . | nindent 4 }}
