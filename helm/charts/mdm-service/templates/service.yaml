apiVersion: v1
kind: Service
metadata:
  name: {{ include "mdm-service.fullname" . }}
  labels: {{- include "mdm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "mdm-service.selectorLabels" . | nindent 4 }}
