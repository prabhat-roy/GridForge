apiVersion: v1
kind: Service
metadata:
  name: {{ include "vpp-service.fullname" . }}
  labels: {{- include "vpp-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "vpp-service.selectorLabels" . | nindent 4 }}
