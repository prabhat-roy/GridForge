apiVersion: v1
kind: Service
metadata:
  name: {{ include "tamper-detection.fullname" . }}
  labels: {{- include "tamper-detection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "tamper-detection.selectorLabels" . | nindent 4 }}
