apiVersion: v1
kind: Service
metadata:
  name: {{ include "rec-service.fullname" . }}
  labels: {{- include "rec-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "rec-service.selectorLabels" . | nindent 4 }}
