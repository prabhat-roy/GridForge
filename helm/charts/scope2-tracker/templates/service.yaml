apiVersion: v1
kind: Service
metadata:
  name: {{ include "scope2-tracker.fullname" . }}
  labels: {{- include "scope2-tracker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "scope2-tracker.selectorLabels" . | nindent 4 }}
