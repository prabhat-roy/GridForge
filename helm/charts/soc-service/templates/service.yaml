apiVersion: v1
kind: Service
metadata:
  name: {{ include "soc-service.fullname" . }}
  labels: {{- include "soc-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "soc-service.selectorLabels" . | nindent 4 }}
