apiVersion: v1
kind: Service
metadata:
  name: {{ include "state-estimator.fullname" . }}
  labels: {{- include "state-estimator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "state-estimator.selectorLabels" . | nindent 4 }}
