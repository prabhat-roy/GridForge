apiVersion: v1
kind: Service
metadata:
  name: {{ include "balancing-mechanism.fullname" . }}
  labels: {{- include "balancing-mechanism.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "balancing-mechanism.selectorLabels" . | nindent 4 }}
