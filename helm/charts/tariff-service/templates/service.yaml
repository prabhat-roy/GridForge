apiVersion: v1
kind: Service
metadata:
  name: {{ include "tariff-service.fullname" . }}
  labels: {{- include "tariff-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "tariff-service.selectorLabels" . | nindent 4 }}
