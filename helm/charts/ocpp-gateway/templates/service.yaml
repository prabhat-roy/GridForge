apiVersion: v1
kind: Service
metadata:
  name: {{ include "ocpp-gateway.fullname" . }}
  labels: {{- include "ocpp-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ocpp-gateway.selectorLabels" . | nindent 4 }}
