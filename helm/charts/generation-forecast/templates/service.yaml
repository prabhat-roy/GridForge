apiVersion: v1
kind: Service
metadata:
  name: {{ include "generation-forecast.fullname" . }}
  labels: {{- include "generation-forecast.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "generation-forecast.selectorLabels" . | nindent 4 }}
