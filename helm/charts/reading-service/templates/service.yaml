apiVersion: v1
kind: Service
metadata:
  name: {{ include "reading-service.fullname" . }}
  labels: {{- include "reading-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "reading-service.selectorLabels" . | nindent 4 }}
