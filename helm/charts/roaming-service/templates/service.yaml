apiVersion: v1
kind: Service
metadata:
  name: {{ include "roaming-service.fullname" . }}
  labels: {{- include "roaming-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "roaming-service.selectorLabels" . | nindent 4 }}
