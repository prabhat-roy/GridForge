apiVersion: v1
kind: Service
metadata:
  name: {{ include "scada-gateway.fullname" . }}
  labels: {{- include "scada-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "scada-gateway.selectorLabels" . | nindent 4 }}
