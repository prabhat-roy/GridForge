apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispatch-service.fullname" . }}
  labels: {{- include "dispatch-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dispatch-service.selectorLabels" . | nindent 4 }}
