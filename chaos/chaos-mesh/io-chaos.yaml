apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata: { name: gridforge-io-latency, namespace: chaos-mesh }
spec:
  action: latency
  mode: one
  selector: { namespaces: [gridforge] }
  volumePath: /var/lib/data
  delay: "100ms"
  percent: 50
  duration: 5m
