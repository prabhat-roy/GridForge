apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata: { name: gridforge-cpu-stress, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [gridforge] }
  stressors: { cpu: { workers: 4, load: 80 } }
  duration: 5m
