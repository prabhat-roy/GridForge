apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata: { name: gridforge-network-loss, namespace: chaos-mesh }
spec:
  action: loss
  mode: one
  selector: { namespaces: [gridforge], labelSelectors: { tier: api } }
  loss: { loss: "10", correlation: "25" }
  duration: 5m
