apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: gridforge-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.gridforge.svc.cluster.local"]
  selector: { namespaces: [gridforge] }
  duration: 2m
