apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: gridforge-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [gridforge] }
  timeOffset: "+500ms"
  duration: 3m
