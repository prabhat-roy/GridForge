apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: gridforge_java_service, title: New GridForge Java/Spring service, tags: [java, gridforge] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties: { name: { title: Name, type: string } }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/java } }
