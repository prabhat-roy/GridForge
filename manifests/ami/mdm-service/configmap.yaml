apiVersion: v1
kind: ConfigMap
metadata:
  name: mdm-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: mdm-service, gridforge.io/domain: ami }
data:
  LOG_LEVEL: info
  PORT: "50102"
