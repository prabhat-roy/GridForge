apiVersion: apps/v1
kind: Deployment
metadata:
  name: mdm-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: mdm-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: ami
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: mdm-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: mdm-service, gridforge.io/domain: ami }
    spec:
      serviceAccountName: mdm-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: mdm-service
          image: harbor.gridforge.internal/gridforge/mdm-service:latest
          ports: [{ containerPort: 50102, name: http }]
          envFrom: [{ configMapRef: { name: mdm-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50102 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
