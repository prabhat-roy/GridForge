apiVersion: v1
kind: Service
metadata:
  name: tamper-detection
  namespace: gridforge
  labels: { app.kubernetes.io/name: tamper-detection, gridforge.io/domain: ami }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: tamper-detection }
