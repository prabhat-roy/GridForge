apiVersion: apps/v1
kind: Deployment
metadata:
  name: reading-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: reading-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: ami
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: reading-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: reading-service, gridforge.io/domain: ami }
    spec:
      serviceAccountName: reading-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: reading-service
          image: harbor.gridforge.internal/gridforge/reading-service:latest
          ports: [{ containerPort: 50101, name: http }]
          envFrom: [{ configMapRef: { name: reading-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50101 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
