apiVersion: v1
kind: Service
metadata:
  name: reading-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: reading-service, gridforge.io/domain: ami }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: reading-service }
