apiVersion: v1
kind: Service
metadata:
  name: bess-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: bess-service, gridforge.io/domain: battery }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: bess-service }
