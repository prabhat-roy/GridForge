apiVersion: v1
kind: ConfigMap
metadata:
  name: bess-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: bess-service, gridforge.io/domain: battery }
data:
  LOG_LEVEL: info
  PORT: "50160"
