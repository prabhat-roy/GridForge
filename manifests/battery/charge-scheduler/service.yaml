apiVersion: v1
kind: Service
metadata:
  name: charge-scheduler
  namespace: gridforge
  labels: { app.kubernetes.io/name: charge-scheduler, gridforge.io/domain: battery }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: charge-scheduler }
