apiVersion: v1
kind: Service
metadata:
  name: soc-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: soc-service, gridforge.io/domain: battery }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: soc-service }
