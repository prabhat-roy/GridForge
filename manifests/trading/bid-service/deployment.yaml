apiVersion: apps/v1
kind: Deployment
metadata:
  name: bid-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: bid-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: trading
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: bid-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: bid-service, gridforge.io/domain: trading }
    spec:
      serviceAccountName: bid-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: bid-service
          image: harbor.gridforge.internal/gridforge/bid-service:latest
          ports: [{ containerPort: 50120, name: http }]
          envFrom: [{ configMapRef: { name: bid-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
