apiVersion: v1
kind: Service
metadata:
  name: bid-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: bid-service, gridforge.io/domain: trading }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: bid-service }
