apiVersion: apps/v1
kind: Deployment
metadata:
  name: imbalance-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: imbalance-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: trading
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: imbalance-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: imbalance-service, gridforge.io/domain: trading }
    spec:
      serviceAccountName: imbalance-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: imbalance-service
          image: harbor.gridforge.internal/gridforge/imbalance-service:latest
          ports: [{ containerPort: 50122, name: http }]
          envFrom: [{ configMapRef: { name: imbalance-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50122 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
