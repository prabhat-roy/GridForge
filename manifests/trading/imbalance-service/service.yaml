apiVersion: v1
kind: Service
metadata:
  name: imbalance-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: imbalance-service, gridforge.io/domain: trading }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: imbalance-service }
