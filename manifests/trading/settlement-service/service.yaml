apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: settlement-service, gridforge.io/domain: trading }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: settlement-service }
