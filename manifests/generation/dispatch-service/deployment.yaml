apiVersion: apps/v1
kind: Deployment
metadata:
  name: dispatch-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: dispatch-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: generation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: dispatch-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: dispatch-service, gridforge.io/domain: generation }
    spec:
      serviceAccountName: dispatch-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: dispatch-service
          image: harbor.gridforge.internal/gridforge/dispatch-service:latest
          ports: [{ containerPort: 50040, name: http }]
          envFrom: [{ configMapRef: { name: dispatch-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50040 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50040 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
