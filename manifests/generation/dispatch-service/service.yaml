apiVersion: v1
kind: Service
metadata:
  name: dispatch-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: dispatch-service, gridforge.io/domain: generation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50040, targetPort: 50040 }]
  selector: { app.kubernetes.io/name: dispatch-service }
