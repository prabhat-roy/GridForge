apiVersion: v1
kind: Service
metadata:
  name: curtailment-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: curtailment-service, gridforge.io/domain: generation }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: curtailment-service }
