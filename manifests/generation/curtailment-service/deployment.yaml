apiVersion: apps/v1
kind: Deployment
metadata:
  name: curtailment-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: curtailment-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: generation
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: curtailment-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: curtailment-service, gridforge.io/domain: generation }
    spec:
      serviceAccountName: curtailment-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: curtailment-service
          image: harbor.gridforge.internal/gridforge/curtailment-service:latest
          ports: [{ containerPort: 50041, name: http }]
          envFrom: [{ configMapRef: { name: curtailment-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50041 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
