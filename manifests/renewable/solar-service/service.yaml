apiVersion: v1
kind: Service
metadata:
  name: solar-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: solar-service, gridforge.io/domain: renewable }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: solar-service }
