apiVersion: apps/v1
kind: Deployment
metadata:
  name: wind-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: wind-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: renewable
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: wind-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: wind-service, gridforge.io/domain: renewable }
    spec:
      serviceAccountName: wind-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: wind-service
          image: harbor.gridforge.internal/gridforge/wind-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: wind-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
