apiVersion: v1
kind: Service
metadata:
  name: wind-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: wind-service, gridforge.io/domain: renewable }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: wind-service }
