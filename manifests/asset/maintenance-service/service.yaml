apiVersion: v1
kind: Service
metadata:
  name: maintenance-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: maintenance-service, gridforge.io/domain: asset }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: maintenance-service }
