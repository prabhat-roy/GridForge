apiVersion: apps/v1
kind: Deployment
metadata:
  name: maintenance-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: maintenance-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: asset
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: maintenance-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: maintenance-service, gridforge.io/domain: asset }
    spec:
      serviceAccountName: maintenance-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: maintenance-service
          image: harbor.gridforge.internal/gridforge/maintenance-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: maintenance-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
