apiVersion: v1
kind: Service
metadata:
  name: inspection-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: inspection-service, gridforge.io/domain: asset }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: inspection-service }
