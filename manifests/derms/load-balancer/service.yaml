apiVersion: v1
kind: Service
metadata:
  name: load-balancer
  namespace: gridforge
  labels: { app.kubernetes.io/name: load-balancer, gridforge.io/domain: derms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50143, targetPort: 50143 }]
  selector: { app.kubernetes.io/name: load-balancer }
