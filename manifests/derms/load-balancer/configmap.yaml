apiVersion: v1
kind: ConfigMap
metadata:
  name: load-balancer
  namespace: gridforge
  labels: { app.kubernetes.io/name: load-balancer, gridforge.io/domain: derms }
data:
  LOG_LEVEL: info
  PORT: "50143"
