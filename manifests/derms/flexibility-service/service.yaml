apiVersion: v1
kind: Service
metadata:
  name: flexibility-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: flexibility-service, gridforge.io/domain: derms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: flexibility-service }
