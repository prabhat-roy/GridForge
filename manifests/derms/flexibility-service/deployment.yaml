apiVersion: apps/v1
kind: Deployment
metadata:
  name: flexibility-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: flexibility-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: derms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: flexibility-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: flexibility-service, gridforge.io/domain: derms }
    spec:
      serviceAccountName: flexibility-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: flexibility-service
          image: harbor.gridforge.internal/gridforge/flexibility-service:latest
          ports: [{ containerPort: 50141, name: http }]
          envFrom: [{ configMapRef: { name: flexibility-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50141 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
