apiVersion: v1
kind: Service
metadata:
  name: vpp-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: vpp-service, gridforge.io/domain: derms }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50144, targetPort: 50144 }]
  selector: { app.kubernetes.io/name: vpp-service }
