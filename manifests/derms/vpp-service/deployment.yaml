apiVersion: apps/v1
kind: Deployment
metadata:
  name: vpp-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: vpp-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: derms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: vpp-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: vpp-service, gridforge.io/domain: derms }
    spec:
      serviceAccountName: vpp-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: vpp-service
          image: harbor.gridforge.internal/gridforge/vpp-service:latest
          ports: [{ containerPort: 50144, name: http }]
          envFrom: [{ configMapRef: { name: vpp-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50144 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
