apiVersion: apps/v1
kind: Deployment
metadata:
  name: der-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: der-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: derms
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: der-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: der-service, gridforge.io/domain: derms }
    spec:
      serviceAccountName: der-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: der-service
          image: harbor.gridforge.internal/gridforge/der-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: der-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
