apiVersion: v1
kind: Service
metadata:
  name: settlement-gateway
  namespace: gridforge
  labels: { app.kubernetes.io/name: settlement-gateway, gridforge.io/domain: market }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: settlement-gateway }
