apiVersion: v1
kind: Service
metadata:
  name: balancing-mechanism
  namespace: gridforge
  labels: { app.kubernetes.io/name: balancing-mechanism, gridforge.io/domain: market }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: balancing-mechanism }
