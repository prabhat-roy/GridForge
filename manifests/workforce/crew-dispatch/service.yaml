apiVersion: v1
kind: Service
metadata:
  name: crew-dispatch
  namespace: gridforge
  labels: { app.kubernetes.io/name: crew-dispatch, gridforge.io/domain: workforce }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: crew-dispatch }
