apiVersion: apps/v1
kind: Deployment
metadata:
  name: work-order-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: work-order-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: workforce
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: work-order-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: work-order-service, gridforge.io/domain: workforce }
    spec:
      serviceAccountName: work-order-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: work-order-service
          image: harbor.gridforge.internal/gridforge/work-order-service:latest
          ports: [{ containerPort: 50260, name: http }]
          envFrom: [{ configMapRef: { name: work-order-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50260 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
