apiVersion: v1
kind: Service
metadata:
  name: work-order-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: work-order-service, gridforge.io/domain: workforce }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: work-order-service }
