apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: api-key-service, gridforge.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
