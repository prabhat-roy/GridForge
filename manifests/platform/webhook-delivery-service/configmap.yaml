apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-delivery-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: webhook-delivery-service, gridforge.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51004"
