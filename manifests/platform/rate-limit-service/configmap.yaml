apiVersion: v1
kind: ConfigMap
metadata:
  name: rate-limit-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: rate-limit-service, gridforge.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51007"
