apiVersion: v1
kind: Service
metadata:
  name: rec-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: rec-service, gridforge.io/domain: carbon }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: rec-service }
