apiVersion: v1
kind: Service
metadata:
  name: scope2-tracker
  namespace: gridforge
  labels: { app.kubernetes.io/name: scope2-tracker, gridforge.io/domain: carbon }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: scope2-tracker }
