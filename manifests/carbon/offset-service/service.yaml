apiVersion: v1
kind: Service
metadata:
  name: offset-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: offset-service, gridforge.io/domain: carbon }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: offset-service }
