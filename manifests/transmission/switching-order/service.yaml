apiVersion: v1
kind: Service
metadata:
  name: switching-order
  namespace: gridforge
  labels: { app.kubernetes.io/name: switching-order, gridforge.io/domain: transmission }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50083, targetPort: 50083 }]
  selector: { app.kubernetes.io/name: switching-order }
