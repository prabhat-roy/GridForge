apiVersion: v1
kind: Service
metadata:
  name: fault-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: fault-service, gridforge.io/domain: transmission }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50082, targetPort: 50082 }]
  selector: { app.kubernetes.io/name: fault-service }
