apiVersion: apps/v1
kind: Deployment
metadata:
  name: fault-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: fault-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: transmission
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: fault-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fault-service, gridforge.io/domain: transmission }
    spec:
      serviceAccountName: fault-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: fault-service
          image: harbor.gridforge.internal/gridforge/fault-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: fault-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
