apiVersion: v1
kind: Service
metadata:
  name: outage-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: outage-service, gridforge.io/domain: transmission }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: outage-service }
