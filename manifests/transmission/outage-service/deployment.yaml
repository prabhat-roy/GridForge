apiVersion: apps/v1
kind: Deployment
metadata:
  name: outage-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: outage-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: transmission
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: outage-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: outage-service, gridforge.io/domain: transmission }
    spec:
      serviceAccountName: outage-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: outage-service
          image: harbor.gridforge.internal/gridforge/outage-service:latest
          ports: [{ containerPort: 50081, name: http }]
          envFrom: [{ configMapRef: { name: outage-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50081 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
