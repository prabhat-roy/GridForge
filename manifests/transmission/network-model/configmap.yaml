apiVersion: v1
kind: ConfigMap
metadata:
  name: network-model
  namespace: gridforge
  labels: { app.kubernetes.io/name: network-model, gridforge.io/domain: transmission }
data:
  LOG_LEVEL: info
  PORT: "50080"
