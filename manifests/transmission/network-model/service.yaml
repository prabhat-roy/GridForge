apiVersion: v1
kind: Service
metadata:
  name: network-model
  namespace: gridforge
  labels: { app.kubernetes.io/name: network-model, gridforge.io/domain: transmission }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: network-model }
