apiVersion: apps/v1
kind: Deployment
metadata:
  name: tariff-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: tariff-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: tariff-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: tariff-service, gridforge.io/domain: customer }
    spec:
      serviceAccountName: tariff-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: tariff-service
          image: harbor.gridforge.internal/gridforge/tariff-service:latest
          ports: [{ containerPort: 50201, name: http }]
          envFrom: [{ configMapRef: { name: tariff-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50201 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
