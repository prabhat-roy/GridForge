apiVersion: v1
kind: Service
metadata:
  name: tariff-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: tariff-service, gridforge.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: tariff-service }
