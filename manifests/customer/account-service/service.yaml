apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: account-service, gridforge.io/domain: customer }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: account-service }
