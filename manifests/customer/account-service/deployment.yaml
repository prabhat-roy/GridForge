apiVersion: apps/v1
kind: Deployment
metadata:
  name: account-service
  namespace: gridforge
  labels:
    app.kubernetes.io/name: account-service
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: customer
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: account-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: account-service, gridforge.io/domain: customer }
    spec:
      serviceAccountName: account-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: account-service
          image: harbor.gridforge.internal/gridforge/account-service:latest
          ports: [{ containerPort: 50200, name: http }]
          envFrom: [{ configMapRef: { name: account-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50200 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
