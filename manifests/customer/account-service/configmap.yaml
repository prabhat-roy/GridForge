apiVersion: v1
kind: ConfigMap
metadata:
  name: account-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: account-service, gridforge.io/domain: customer }
data:
  LOG_LEVEL: info
  PORT: "50200"
