apiVersion: v1
kind: Service
metadata:
  name: load-forecast
  namespace: gridforge
  labels: { app.kubernetes.io/name: load-forecast, gridforge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50280, targetPort: 50280 }]
  selector: { app.kubernetes.io/name: load-forecast }
