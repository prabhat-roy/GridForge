apiVersion: apps/v1
kind: Deployment
metadata:
  name: load-forecast
  namespace: gridforge
  labels:
    app.kubernetes.io/name: load-forecast
    app.kubernetes.io/part-of: gridforge
    gridforge.io/domain: analytics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: load-forecast } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: load-forecast, gridforge.io/domain: analytics }
    spec:
      serviceAccountName: load-forecast
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: load-forecast
          image: harbor.gridforge.internal/gridforge/load-forecast:latest
          ports: [{ containerPort: 50280, name: http }]
          envFrom: [{ configMapRef: { name: load-forecast } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50280 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
