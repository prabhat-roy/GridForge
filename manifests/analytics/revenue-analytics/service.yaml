apiVersion: v1
kind: Service
metadata:
  name: revenue-analytics
  namespace: gridforge
  labels: { app.kubernetes.io/name: revenue-analytics, gridforge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: revenue-analytics }
