apiVersion: v1
kind: Service
metadata:
  name: generation-forecast
  namespace: gridforge
  labels: { app.kubernetes.io/name: generation-forecast, gridforge.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: generation-forecast }
