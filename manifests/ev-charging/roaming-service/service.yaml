apiVersion: v1
kind: Service
metadata:
  name: roaming-service
  namespace: gridforge
  labels: { app.kubernetes.io/name: roaming-service, gridforge.io/domain: ev-charging }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: roaming-service }
