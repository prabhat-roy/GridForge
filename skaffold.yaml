# Skaffold pipeline for GridForge
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: gridforge }
build:
  artifacts:
    - { image: gridforge/api-gateway, context: src/api-gateway }
    - { image: gridforge/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: gridforge }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
